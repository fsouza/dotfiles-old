DOTFILES_DIR=${basedir}

function copy_ocamlformat {
	if ! [ -f .ocamlformat ]; then
		cp "${DOTFILES_DIR}/.ocamlformat" .
	fi
}

if command -v opam &>/dev/null && [ -f "$HOME/.opam/opam-init/init.zsh" ]; then
	source "$HOME/.opam/opam-init/init.zsh"
fi
