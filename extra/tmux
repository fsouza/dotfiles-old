function _go_to_tmux {
	if command tmux has-session; then
		command tmux -f $HOME/.config/tmux.conf attach
	else
		command tmux -f $HOME/.config/tmux.conf new
	fi
}

function tmux {
	_go_to_tmux
}

if [ -z "$TMUX" ] && [ -n "$(which tmux)" ] && [[ $- =~ i ]]; then
	_go_to_tmux
fi
